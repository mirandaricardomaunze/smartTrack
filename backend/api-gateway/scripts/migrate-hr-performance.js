'use strict';const pool=require('../src/infrastructure/db');async function run(){const c=await pool.connect();try{await c.query(`CREATE TABLE IF NOT EXISTS hr_performance_reviews(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',employee_id TEXT NOT NULL,period TEXT NOT NULL,scores JSONB NOT NULL DEFAULT '{}',final_score NUMERIC(3,2) NOT NULL DEFAULT 0,goals JSONB NOT NULL DEFAULT '[]',feedback TEXT,development_plan TEXT,status TEXT NOT NULL DEFAULT 'draft',reviewer_id TEXT,completed_by TEXT,completed_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(company_id,employee_id,period))`);console.info('[migrate-hr-performance] Tabela pronta.');}finally{c.release();await pool.end();}}run().catch(e=>{console.error(e.message);process.exitCode=1;});
