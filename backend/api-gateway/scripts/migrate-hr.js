'use strict';
const pool=require('../src/infrastructure/db');
async function run(){const client=await pool.connect();try{await client.query('BEGIN');await client.query(`CREATE TABLE IF NOT EXISTS hr_departments(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',name TEXT NOT NULL,code TEXT NOT NULL,manager_name TEXT,description TEXT,status TEXT NOT NULL DEFAULT 'active',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(company_id,code))`);await client.query(`CREATE TABLE IF NOT EXISTS hr_employees(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',employee_number TEXT NOT NULL,full_name TEXT NOT NULL,email TEXT,phone TEXT,tax_id TEXT,birth_date DATE,gender TEXT,address JSONB,emergency_contact JSONB,department_id TEXT,job_title TEXT NOT NULL,employment_type TEXT NOT NULL DEFAULT 'permanent',hire_date DATE NOT NULL,salary_cents INTEGER NOT NULL DEFAULT 0,bank_details JSONB,status TEXT NOT NULL DEFAULT 'active',notes TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(company_id,employee_number))`);await client.query(`CREATE TABLE IF NOT EXISTS hr_leave_requests(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',employee_id TEXT NOT NULL,type TEXT NOT NULL,start_date DATE NOT NULL,end_date DATE NOT NULL,days INTEGER NOT NULL,reason TEXT,status TEXT NOT NULL DEFAULT 'pending',decision_notes TEXT,decided_by TEXT,decided_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await client.query(`CREATE TABLE IF NOT EXISTS hr_attendance(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',employee_id TEXT NOT NULL,work_date DATE NOT NULL,clock_in TIMESTAMPTZ,clock_out TIMESTAMPTZ,break_minutes INTEGER NOT NULL DEFAULT 60,worked_minutes INTEGER NOT NULL DEFAULT 0,late_minutes INTEGER NOT NULL DEFAULT 0,overtime_minutes INTEGER NOT NULL DEFAULT 0,status TEXT NOT NULL DEFAULT 'present',notes TEXT,adjusted_by TEXT,adjusted_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(company_id,employee_id,work_date))`);await client.query(`CREATE INDEX IF NOT EXISTS idx_hr_employees_company_status ON hr_employees(company_id,status)`);await client.query(`CREATE INDEX IF NOT EXISTS idx_hr_leave_company_status ON hr_leave_requests(company_id,status)`);await client.query(`CREATE INDEX IF NOT EXISTS idx_hr_attendance_company_date ON hr_attendance(company_id,work_date)`);await client.query('COMMIT');console.info('[migrate-hr] Tabelas de RH prontas.');}catch(e){await client.query('ROLLBACK');throw e;}finally{client.release();await pool.end();}}
run().catch(e=>{console.error('[migrate-hr]',e.message);process.exitCode=1;});
