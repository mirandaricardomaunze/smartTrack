NODE_ENV=development
PORT=4002

# Cada microsserviço tem a sua própria base — não partilhar com o api-gateway
PGHOST=localhost
PGPORT=5432
PGDATABASE=routes_db
PGUSER=postgres
PGPASSWORD=

# Barramento de eventos (ainda não consumido — ver docs/adr/ADR-001-stack.md)
KAFKA_BROKERS=localhost:9092
