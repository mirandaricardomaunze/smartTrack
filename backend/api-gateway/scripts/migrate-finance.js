'use strict';const pool=require('../src/infrastructure/db');async function run(){const c=await pool.connect();try{await c.query('BEGIN');await c.query(`CREATE TABLE IF NOT EXISTS finance_accounts(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',code TEXT NOT NULL,name TEXT NOT NULL,category TEXT NOT NULL,parent_id TEXT,active BOOLEAN NOT NULL DEFAULT TRUE,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(company_id,code))`);await c.query(`CREATE TABLE IF NOT EXISTS finance_entries(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',type TEXT NOT NULL,description TEXT NOT NULL,party_name TEXT,document_number TEXT,account_id TEXT,amount_cents INTEGER NOT NULL,due_date DATE NOT NULL,status TEXT NOT NULL DEFAULT 'open',paid_at TIMESTAMPTZ,payment_method TEXT,payment_reference TEXT,created_by TEXT,settled_by TEXT,voided_by TEXT,voided_at TIMESTAMPTZ,notes TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await c.query(`CREATE INDEX IF NOT EXISTS idx_finance_entries_company_due ON finance_entries(company_id,status,due_date)`);await c.query('COMMIT');console.info('[migrate-finance] Tabelas prontas.');}catch(e){await c.query('ROLLBACK');throw e;}finally{c.release();await pool.end();}}run().catch(e=>{console.error(e.message);process.exitCode=1;});
