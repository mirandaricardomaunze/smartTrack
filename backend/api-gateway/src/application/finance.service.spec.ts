import{describe,expect,it}from'vitest';import{FinanceEntryFactory}from'../../../../tests/harness';const{validateEntry}=require('./finance.service');describe('Finance entry validation',()=>{it('should normalize a valid receivable expressed in cents',()=>{const e=FinanceEntryFactory.buildReceivable();expect(validateEntry(e)).toMatchObject(e);});it('should reject non-positive financial amounts',()=>{const e=FinanceEntryFactory.buildPayable({amount_cents:0});expect(()=>validateEntry(e)).toThrow();});});
