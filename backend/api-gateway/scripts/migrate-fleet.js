'use strict';const pool=require('../src/infrastructure/db');async function run(){const c=await pool.connect();try{await c.query('BEGIN');await c.query(`CREATE TABLE IF NOT EXISTS fleet_vehicles(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',plate TEXT NOT NULL,make TEXT NOT NULL,model TEXT NOT NULL,year INTEGER,vehicle_type TEXT,fuel_type TEXT NOT NULL,odometer_km INTEGER NOT NULL DEFAULT 0,status TEXT NOT NULL DEFAULT 'available',insurance_expiry DATE,inspection_expiry DATE,notes TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(company_id,plate))`);await c.query(`CREATE TABLE IF NOT EXISTS fleet_fuel_entries(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',vehicle_id TEXT NOT NULL,fuel_date DATE NOT NULL,odometer_km INTEGER NOT NULL,volume_ml INTEGER NOT NULL,cost_cents INTEGER NOT NULL,full_tank BOOLEAN NOT NULL DEFAULT TRUE,station TEXT,driver_id TEXT,consumption_l_per_100km NUMERIC(8,2),created_by TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await c.query(`CREATE INDEX IF NOT EXISTS idx_fleet_fuel_vehicle_date ON fleet_fuel_entries(company_id,vehicle_id,fuel_date)`);await c.query('COMMIT');console.info('[migrate-fleet] Tabelas prontas.');}catch(e){await c.query('ROLLBACK');throw e;}finally{c.release();await pool.end();}}run().catch(e=>{console.error(e.message);process.exitCode=1;});
