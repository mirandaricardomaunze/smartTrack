'use strict';const pool=require('../src/infrastructure/db');async function run(){const c=await pool.connect();try{await c.query('BEGIN');await c.query(`CREATE TABLE IF NOT EXISTS hr_payroll_runs(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',period TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'draft',employee_count INTEGER NOT NULL DEFAULT 0,gross_cents BIGINT NOT NULL DEFAULT 0,deductions_cents BIGINT NOT NULL DEFAULT 0,net_cents BIGINT NOT NULL DEFAULT 0,approved_by TEXT,approved_at TIMESTAMPTZ,paid_by TEXT,paid_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(company_id,period))`);await c.query(`CREATE TABLE IF NOT EXISTS hr_payroll_items(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'company-default',payroll_id TEXT NOT NULL,employee_id TEXT NOT NULL,base_salary_cents INTEGER NOT NULL DEFAULT 0,allowances_cents INTEGER NOT NULL DEFAULT 0,bonus_cents INTEGER NOT NULL DEFAULT 0,overtime_cents INTEGER NOT NULL DEFAULT 0,tax_cents INTEGER NOT NULL DEFAULT 0,social_security_cents INTEGER NOT NULL DEFAULT 0,other_deductions_cents INTEGER NOT NULL DEFAULT 0,gross_cents INTEGER NOT NULL DEFAULT 0,deductions_cents INTEGER NOT NULL DEFAULT 0,net_cents INTEGER NOT NULL DEFAULT 0,notes TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(company_id,payroll_id,employee_id))`);await c.query('COMMIT');console.info('[migrate-hr-payroll] Tabelas prontas.');}catch(e){await c.query('ROLLBACK');throw e;}finally{c.release();await pool.end();}}run().catch(e=>{console.error(e.message);process.exitCode=1;});
