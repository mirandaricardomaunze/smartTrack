'use strict';const pool=require('../src/infrastructure/db');async function run(){const c=await pool.connect();try{await c.query('BEGIN');await c.query(`ALTER TABLE hr_employees ADD COLUMN IF NOT EXISTS user_id TEXT; CREATE UNIQUE INDEX IF NOT EXISTS uq_hr_employees_user ON hr_employees(user_id) WHERE user_id IS NOT NULL; CREATE INDEX IF NOT EXISTS idx_hr_employees_company_user ON hr_employees(company_id,user_id);`);await c.query('COMMIT');console.info('[migrate-hr-portal] Vínculo de contas pronto.');}catch(e){await c.query('ROLLBACK');throw e;}finally{c.release();await pool.end();}}run().catch(e=>{console.error(e.message);process.exitCode=1;});
