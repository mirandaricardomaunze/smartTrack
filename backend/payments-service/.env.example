NODE_ENV=development
PORT=4003

# Cada microsserviço tem a sua própria base
PGHOST=localhost
PGPORT=5432
PGDATABASE=payments_db
PGUSER=postgres
PGPASSWORD=

# Segredo de verificação dos webhooks do gateway.
# Sem ele, TODOS os webhooks são recusados (falha fechada, nunca aberta).
PAYMENTS_WEBHOOK_SECRET=

# Credenciais dos gateways reais — nenhum está ligado ainda.
# Ver src/infrastructure/gateway.client.js para a costura de integração.
# MERCADO_PAGO_ACCESS_TOKEN=
# STRIPE_SECRET_KEY=

KAFKA_BROKERS=localhost:9092
