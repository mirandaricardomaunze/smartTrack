import{describe,expect,it}from'vitest';import{FuelConsumptionFactory}from'../../../../tests/harness';const{calculateConsumption}=require('./fleet.service');describe('Fleet fuel consumption',()=>{it('should calculate litres per 100 kilometres between full tanks',()=>{const f=FuelConsumptionFactory.build();expect(calculateConsumption(f.previous_odometer_km,f.odometer_km,f.volume_ml)).toBe(f.consumption_l_per_100km);});it('should return null when odometer does not advance',()=>{const f=FuelConsumptionFactory.build({odometer_km:50000});expect(calculateConsumption(f.previous_odometer_km,f.odometer_km,f.volume_ml)).toBeNull();});});
